---
- name: Ansbile module for (debugging and troubleshooting)
  hosts: webservers
  become: true
  
  tasks:
    - name: Print message
      ansible.builtin.debug:
        msg: "Hello from Ansible"
    
    - name: Display variable value
      ansible.builtin.debug:
        var: ansible_facts['hostname']
