---
- name: Ansible module for (service management)
  hosts: webservers
  become: true

  tasks:
    - name: Start and enable Apache2 service
      ansible.builtin.service:
        name: apache2
        state: started
        enabled: true
